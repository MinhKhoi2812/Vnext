import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
	root: {
		marginTop: "0.72rem",
		marginRight:'10rem'
	},
	searchComponent: {
		marginRight: 10,
	},
	groupComponnent: {
		height: '100%',
		width: '100%'
	},
}));
