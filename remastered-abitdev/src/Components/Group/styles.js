import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => ({
	root: {
		marginTop: "0.72rem",
	
	},
	searchComponent:{
		marginRight: 15
	},
	groupComponnent:{
		height: '620px',
	}
}))
