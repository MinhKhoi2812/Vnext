import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
	typography: {
		textAlign: "center",
		fontSize: "23px",
		color: "black",
		marginTop: "2rem",
	},
	buttonRight: {
		marginTop: '2rem',
		marginLeft:'16rem'
	}
}));
